<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <a href="{{ path('joueur_add') }}">Créer</a>
    {% for joueur in joueurs %}
        <h1>{{joueur.prenom}} {{joueur.nom}}</h1>
        <a href="{{ path('joueur_focus', {'id' : joueur.id}) }}"><img style="width: 500px" src="{{joueur.src}}" alt=""></a>
        <br>
        <a href="{{ path('joueur_focus', {'id' : joueur.id}) }}">Lire</a>
        <a href="{{ path('joueur_update', {'id' : joueur.id}) }}">Modifier</a>
        <a href="{{ path('joueur_delete', {'id' : joueur.id}) }}">Supprimer</a>
    {% endfor %}
</body>
</html>